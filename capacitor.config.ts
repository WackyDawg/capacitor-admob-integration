import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.admobint.app',
  appName: 'capacitor-admob-integration',
  webDir: 'dist/capacitor-admob-integration/browser'
};

export default config;
